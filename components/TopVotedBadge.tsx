// Small champion badge for the single most-upvoted product.
export default function TopVotedBadge() {
  return (
    <span className="mono inline-block shrink-0 bg-[var(--ink)] px-1.5 py-0.5 text-[10px] tracking-[0.15em] text-[var(--paper)]">
      ★ TOP VOTED
    </span>
  )
}
